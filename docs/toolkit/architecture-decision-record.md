# 架构决策记录：是否采用多 Agent

复制本页作为新 ADR，并将文件命名为 `ADR-NNN-简短标题.md`。

## 状态

提议 / 已接受 / 已拒绝 / 已替代

## 背景

业务目标、用户、任务类型和当前瓶颈。

## 约束

延迟、成本、权限、合规、数据、团队和运维约束。

## 候选方案

1. 确定性工作流；
2. LLM 增强工作流；
3. 单 Agent；
4. 多 Agent。

## 决策

选择哪一项，为什么它是满足需求的最简单架构。

## 真实边界

逐项说明领域、数据、工具、权限、状态、故障、扩缩容和组织所有权边界。

## 代价

预计增加的模型调用、延迟、Token、状态同步、测试面和运维复杂度。

## 验证

用哪些 Golden Dataset、指标、基线和上线门禁证明收益。

## 回退

收益不达标或风险不可控时，如何降级为更简单的架构。
